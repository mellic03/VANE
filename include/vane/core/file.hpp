#pragma once

#include <vane/types.hpp>
#include <string>
#include <vector>


namespace vane
{
    // template <typename T>
    // T loadFileRaw( const std::string& );

    // template<>
    // std::vector<uint8>loadRaw( const std::string& );

    // template<>
    std::string loadRaw( const std::string& );
}
